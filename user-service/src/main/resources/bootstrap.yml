spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      name: user-service
      profile: default

# Указываем JDBC URL для миграций Flyway (должен быть доступен при запуске)
USER_DB_JDBC_URL: jdbc:postgresql://${postgres-user:localhost}:5432/userdb
USER_DB_URL: r2dbc:postgresql://${postgres-user:localhost}:5432/userdb
USER_DB_USER: postgres
USER_DB_PASS: postgres