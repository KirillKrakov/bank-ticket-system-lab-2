server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
  instance:
    hostname: gateway
    prefer-ip-address: false